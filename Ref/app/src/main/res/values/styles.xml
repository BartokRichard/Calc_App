<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Alkalmazás alapértelmezett témája -->
    <style name="Theme.CalculatorApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Az alkalmazás itt testreszabható -->
        <item name="colorPrimary">@color/background_color</item>
        <item name="colorPrimaryVariant">@color/soft_purple</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="android:statusBarColor">@color/background_color</item>
        <item name="android:navigationBarColor">@color/nav_background_color</item>
    </style>

</resources>
